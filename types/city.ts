export interface City {
  city_id: string;
  city_name: string;
  province_id: string;
  province: string;
  type: string;
  postal_code: string;
}

export interface Role {
  id: string;
  role_name: string;
}
